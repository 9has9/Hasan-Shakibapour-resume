📄 Resume Project – Internet Engineering

این پروژه شامل طراحی رزومه آنلاین، بک‌اند با FastAPI، سیستم درخواست پروژه، پنل ادمین و نمایش تعداد کاربران آنلاین به‌صورت زنده با WebSocket است.

🔗 لینک رزومه (GitHub Pages)

رزومه آنلاین پروژه از طریق GitHub Pages در دسترس است:

👉 https://9has9.github.io/Hasan-Shakibapour-resume/

توجه: برای عملکرد کامل (ارسال فرم، تعداد کاربران آنلاین، پنل ادمین)، بک‌اند باید به‌صورت لوکال اجرا شود.

ساختار پروژه
Hasan-Shakibapour-resume/
│
├── backend/
│   ├── main.py
│   ├── models.py
│   ├── schemas.py
│   ├── database.py
│   ├── auth.py
│   ├── requirements.txt
│
├── admin/
│   ├── login.html
│   ├── dashboard.html
│   ├── admin.js
│   ├── dashboard.js
│
├── index.html
├── assets/
│
└── README.md

⚙️ اجرای بک‌اند (FastAPI)
1️⃣ پیش‌نیازها

Python 3.9 یا بالاتر

pip

2️⃣ نصب وابستگی‌ها
cd backend
pip install -r requirements.txt

3️⃣ اجرای سرور FastAPI
uvicorn main:app --reload


بعد از اجرا:

API در آدرس زیر فعال است:

http://127.0.0.1:8000

مستندات Swagger:
http://127.0.0.1:8000/docs

🧪 تست فرم درخواست پروژه

فرم درخواست پروژه در صفحه رزومه قرار دارد.
با پر کردن فرم، درخواست به بک‌اند ارسال و در دیتابیس ذخیره می‌شود.

🔐 پنل ادمین
📍 اجرای فرانت ادمین (ضروری)

برای جلوگیری از مشکل CORS و redirect، فرانت باید با سرور محلی اجرا شود:

از ریشه پروژه:

python -m http.server 5500


سپس در مرورگر:

http://localhost:5500/admin/login.html

🔑 اطلاعات ورود ادمین
Username: admin
Password: admin123


(در صورت تغییر، اطلاعات در دیتابیس تعریف شده‌اند)

🧭 امکانات پنل ادمین

مشاهده تمام درخواست‌های ارسال‌شده

تغییر وضعیت درخواست‌ها

احراز هویت با JWT

ارتباط کامل با بک‌اند

👥 نمایش کاربران آنلاین (WebSocket)

تعداد کاربران آنلاین به‌صورت زنده با WebSocket نمایش داده می‌شود

با باز شدن هر تب جدید، تعداد افزایش می‌یابد

با بستن تب، تعداد کاهش می‌یابد

این قابلیت روی صفحه رزومه قابل مشاهده است.

🎥 ارائه پروژه (ویدیو)

در ویدیوی ارائه، موارد زیر نمایش داده می‌شود:

رزومه آنلاین (GitHub Pages)

ارسال فرم درخواست پروژه

اجرای بک‌اند و نمایش Swagger

ورود به پنل ادمین و مشاهده درخواست‌ها

نمایش تعداد کاربران آنلاین به‌صورت زنده

🛠️ تکنولوژی‌های استفاده‌شده

FastAPI

SQLAlchemy

JWT Authentication

WebSocket

HTML / CSS / JavaScript

GitHub Pages

👤 دانشجو

نام: حسن شکیباپور
شماره دانشجویی:۴۰۱۷۳۱۰۱
درس: مهندسی اینترنت
